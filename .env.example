# K-ID Age Verifier Environment Configuration

# Cloudflare Workers Proxy Settings (optional)
# Set to true and provide credentials if deploying to Cloudflare Workers
ENABLE_WORKER_PROXY=false
WORKER_PROXY_TOKEN=
WORKER_PROXY_URL=

# Local Development Settings
# For local testing, no additional configuration needed
# Just run: npx tsx scripts/standalone-verify.ts "YOUR_K-ID_URL"
